{
  "name": "glossary",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass": "node_modules/.bin/node-sass --recursive --output css --source-map true --source-map-contents scss",
    "build": "npm run sass && eleventy .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "eleventy": "^1.0.6",
    "node-sass": "^7.0.0",
    "spectre.css": "^0.5.8"
  }
}
